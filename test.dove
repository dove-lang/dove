class Person {
    fun say_hi() {
        print "hi " + self.name

        let count = 0

        fun say_hi_count() {
            count = count + 1
            print "hi " + self.name
            print count
        }

        return say_hi_count
    }

    fun get_self() {
        return self
    }
}

let person = Person()
person.name = "Hmm"

let count = person.say_hi()
count()
count()

print person.get_self().get_self().get_self().name
